# ── Exportação openHistorian ──────────────────────────────────────────────────
openhistorian>=2.8

# ── Dados / IO ────────────────────────────────────────────────────────────────
pyarrow>=17.0
pandas>=2.2
numpy>=1.26

# ── ML / Treinamento ──────────────────────────────────────────────────────────
torch>=2.5
transformers>=4.47
accelerate>=1.0
scikit-learn>=1.5

# ── TSPulse / TSFM (IBM Granite) ─────────────────────────────────────────────
# Instalar via pip no ambiente vast.ai:
#   pip install "granite-tsfm[notebooks] @ git+https://github.com/ibm-granite/granite-tsfm.git@v0.3.1"
# Ou para a versão mais recente:
#   pip install tsfm-public
momentfm

# ── Utilitários ───────────────────────────────────────────────────────────────
tqdm>=4.66
tensorboard>=2.17
